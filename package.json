{
  "name": "fullstack-insight-workspace",
  "version": "1.0.0",
  "description": "Unified workspace for FullStack Insight analytics app",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend --production=false",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "author": "Antigravity",
  "license": "ISC"
}
